<ul class="nav navbar-nav navbar-left">
    @if(Request.IsAuthenticated) {
    <li class="dropdown">
        <a href="#" class="dropdown-toggle dropdown-avatar" data-toggle="dropdown">
            <span>
                <span>@User.Identity.Name <i class="icon-caret-down"></i></span>
            </span>
        </a>
        <ul class="dropdown-menu">
            <li class="with-image">
                <span>@User.Identity.Name</span>
            </li>

            <li class="divider"></li>

            <li><a href="@Url.Action("Index", "Profile", new { area = "Settings" })"><i class="icon-user"></i> <span>Profile</span></a></li>
            <li><a href="@Url.Action("ChangePassword", "Account", new{ area = "" })"><i class="icon-cog"></i> <span>Change Password</span></a></li>
            <li><a href="@Url.Action("LogOff", "Account", new { area = "" })"><i class="icon-off"></i> <span>Logout</span></a></li>
        </ul>
    </li>
    }
    else
    {
        <li>
            <a href="@Url.Action("LogOn", "Account", new { area = "" })">
                <i class="icon-user"></i> <span>Log On</span>
            </a>
        </li>
    }
</ul>